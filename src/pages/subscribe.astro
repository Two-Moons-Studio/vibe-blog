---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="Subscribe - Zero to Vibe Coder" 
  description="Get updates on the journey from designer to AI-assisted builder"
>
  <div class="container">
    <article class="subscribe-page">
      <header class="page-header">
        <h1>Subscribe</h1>
        <p>Get updates on the journey from Figma to functional</p>
      </header>

      <div class="subscribe-content">
        <div class="value-prop">
          <h2>What You'll Get</h2>
          <ul>
            <li><strong>Weekly-ish updates</strong> when I learn something worth sharing</li>
            <li><strong>Behind-the-scenes</strong> of building products with AI</li>
            <li><strong>Designer perspective</strong> on technical challenges</li>
            <li><strong>PM insights</strong> on what's actually worth building</li>
            <li><strong>Reality checks</strong> on time, cost, and sanity</li>
          </ul>
        </div>

        <div class="signup-form">
          <!-- TODO: Replace with actual Buttondown embed -->
          <div class="buttondown-placeholder">
            <p><strong>Buttondown signup form will go here</strong></p>
            <p class="setup-note">
              To set up: Create Buttondown account → Get embed code → Replace this placeholder
            </p>
          </div>
        </div>

        <div class="promises">
          <h3>The Promise</h3>
          <p>
            No BS. No growth hacking. No "scaling your agency with AI" nonsense. 
            Just honest documentation of what it's really like to learn this stuff 
            as a designer-turned-PM.
          </p>
          <p>
            Unsubscribe anytime. Your email stays private. 
            No spam, no selling your data to growth gurus.
          </p>
        </div>
      </div>
    </article>
  </div>
</Layout>

<style>
  .subscribe-page {
    padding: var(--space-8) 0;
  }

  .page-header {
    text-align: center;
    margin-bottom: var(--space-8);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-neutral-20);
  }

  .page-header h1 {
    margin-bottom: var(--space-3);
  }

  .page-header p {
    font-size: var(--font-size-large);
    color: var(--color-text-secondary);
    margin: 0;
  }

  .subscribe-content {
    max-width: 500px;
    margin: 0 auto;
  }

  .value-prop {
    margin-bottom: var(--space-8);
  }

  .value-prop h2 {
    margin-bottom: var(--space-4);
  }

  .signup-form {
    background: var(--color-neutral-20);
    padding: var(--space-6);
    border-radius: 8px;
    margin-bottom: var(--space-8);
    text-align: center;
  }

  .buttondown-placeholder {
    color: var(--color-text-secondary);
  }

  .setup-note {
    font-size: var(--font-size-small);
    font-style: italic;
    margin-top: var(--space-3);
    margin-bottom: 0;
  }

  .promises {
    text-align: center;
    padding-top: var(--space-6);
    border-top: 1px solid var(--color-neutral-20);
  }

  .promises h3 {
    margin-bottom: var(--space-4);
  }

  .promises p {
    color: var(--color-text-secondary);
    margin: 0 auto var(--space-3);
    max-width: 400px;
  }

  .promises p:last-child {
    margin-bottom: 0;
  }
</style>